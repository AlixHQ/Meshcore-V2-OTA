# Meshcore V2 OTA

Personal firmware transport edge. Not a general download or support repository.

Only app-only firmware listed in `SHA256SUMS` is suitable for URL OTA. Never use a merged/full image as an OTA input.
